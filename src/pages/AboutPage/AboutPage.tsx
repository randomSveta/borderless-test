import { Typography } from "@mui/material";

const AboutPage: React.FC = () => {
  return (
    <Typography variant="body2" className="AboutPage" data-testid="about-text">
      Oh well, this one is empty for now ヽ(•‿•)ノ
    </Typography>
  );
};

export default AboutPage;
